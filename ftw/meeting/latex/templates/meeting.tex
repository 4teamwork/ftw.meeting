\section*{${title}}

<%block name="top">
</%block>

% if meetingForm:
  {\bf ${meetingForm}} \\ %%
% endif

\vspace{\baselineskip}

<%block name="table">
{
\setlength\tabcolsep{0pt}
\begin{tabular}{p{.3\textwidth}p{.7\textwidth}}
  % for label, value in metadata:
    {\bf ${label}} & ${value} \\ %%
  % endfor
  % if meetingItems:
    {\bf ${_(u'latex_label_agenda_items', default=u'Agenda Items')}} & {
    \vspace{-0.8\baselineskip} \begin{enumerate}[leftmargin=*]
    % for item in meetingItems:
      \item{${item}}
    % endfor
    \end{enumerate}}
  % endif
\end{tabular}
}
</%block>

<%block name="content">
${latex_content}
</%block>
